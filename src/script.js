

let nameList = "https://api.agify.io?name[]=michael&name[]=matthew&name[]=jane";


let firstName = prompt("Your name is");
firstName = firstName.toLowerCase();
if ([firstName] !== undefined) {
    let currentAge = [firstName];
}